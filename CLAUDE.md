# [Claude Code 작업 지시서] 사양서 검색 사이트 (부품 Spec. Sheet 검색)

- 기획자: 류제찬 (HD건설기계)
- 출처: 생성형 AI 업무자동화 전문가과정 [1차수] — 수강생 프로젝트 기획서 17번
- 프로젝트 유형: 사내 문서 검색 웹 (엑셀 폴더 → 색인 → 검색 → 사양서 화면 출력)
- 대체 대상: **메인 서버 공유 폴더에 쌓인 사양서 엑셀 파일**을 사람이 눈으로 뒤지는 일

---

## 0. Claude Code에게 — 작업 방식

1. 기획자가 **실제 사양서 10건을 압축해 함께 보냈다.** 화면을 상상하기 전에
   그 파일들을 먼저 열어 볼 것. 이 지시서의 §3 은 전부 그 10건을 실제로
   파싱해 보고 적은 것이다.
2. 진짜 문제는 검색창이 없다는 것이 아니라 **사양서 양식이 일정하지 않다**는 것이다.
   같은 폴더에 들어 있는 10건이 이미 이렇게 다르다.
   - 시트 이름이 7종이다 (`부품사양서` / `(양식) 부품사양서` / `부품사양서 (양식)` /
     `부품사양` / `구매목록` / `에어 호스` …)
   - 제목 줄이 한 칸씩 밀려 **품번이 B3 인 파일과 B4 인 파일**이 섞여 있다
   - **같은 항목을 다른 이름**으로 부른다 — `제조 Maker` = `메이커`,
     `모델 및 규격` = `규격 및 모델`(어순이 뒤집혀 있다)
   - 10건 중 **1건은 사양서가 아니다** (`구매 목록`)
   - 10건 중 **2건은 파일명의 품번과 내용의 품번이 다르다**
   위치를 믿고 `C4` 를 읽는 파서는 10건 중 2건을 **조용히** 틀리게 읽는다.
   틀렸다는 사실조차 남지 않는 것이 가장 나쁘다.
3. 규칙은 `js/logic.js`(UMD)에 모아 Node 단위 테스트로 고정하고,
   **엑셀을 읽는 규칙은 `scripts/build_index.py` 에 두고 `test/test_build_index.py` 로**
   따로 고정한다. 두 곳의 규칙 이름을 일치시킨다.
4. **사내 실데이터를 저장소에 넣지 않는다.** 기획자가 보낸 10건에는
   담당자 이름·휴대전화·회사 이메일이 들어 있다. 품번 체계와 항목 구조는 살리고,
   값은 전부 `scripts/gen-seed.py` 가 만든 샘플을 쓴다.
5. 기획서 원문(부록 A)이 최종 기준이다.

---

## 1. 한 줄 요약

메인 서버 폴더에 쌓인 **사양서 엑셀을 색인해 두고**, 사용자가 검색창에 품번이나
"호스 검색해줘" 같은 말을 넣으면 **사양서 내용을 화면에 그대로 펼쳐 주고**,
없으면 **등록을 요청하라고 안내**하는 사내 검색 사이트.

## 2. 배경 및 현재 업무 상황

- 부품 사양서(Spec. Sheet)는 품번마다 한 장씩 있는 **엑셀 시트**다.
  품번·품명·모델 및 규격·제조 Maker·재질·용도·상세규격과 **부품 사진**이 들어 있다.
- 지금은 메인 서버 공유 폴더에 파일로 쌓여 있고, 필요한 사람이 **폴더를 눈으로 뒤진다.**
- 파일명이 `420108-02540 HOSE,GAS;OXYGEN TWIN _FM USE.xlsx` 처럼 되어 있어
  **품번을 알면** 찾을 수 있지만, **품번을 모르면 못 찾는다.**
  "산소호스가 필요한데 품번이 뭐지"가 실제로 막히는 지점이다.
- 기획자 원문: *"메인 서버 특정 폴더(D/B?)에 사양서를 엑셀 파일로 저장 해두고"* —
  물음표가 붙어 있다. **DB 로 옮겨야 하는지 자체가 질문이었다.** §4 에서 답한다.

## 3. 기획자가 보낸 10건에서 실제로 읽은 것

이 표는 상상이 아니라 파싱 결과다. 파서를 고칠 때 이 표를 기준으로 삼는다.

| 항목 | 10건 중 | 비고 |
|---|---|---|
| 품번 | 10 | 유일하게 전부 있다. 형식은 **6자리-5자리** (`420108-02540`) |
| 품명 | 10 | 영문 표기 (`HOSE,GAS;OXYGEN TWIN _FM USE`) |
| 용도 | 9 | 한글 문장. **검색이 실제로 걸리는 칸** |
| 모델 및 규격 | 6 (+3은 `규격 및 모델`) | 어순이 뒤집힌 같은 항목 |
| 제조 Maker | 6 (+3은 `메이커`) | 같은 항목, 다른 이름 |
| 구매단위·단위당 수량·재질·원산지·상세규격·Remark | 6 | 표준 양식에만 |
| 주문자 연락처 | 6 | **개인정보** — 이름/휴대전화/이메일 |

세 가지 문서가 한 폴더에 섞여 있다.

| 종류 | 건수 | 어떻게 알아보나 |
|---|---|---|
| 부품 사양서 | 8 | 제목 칸에 `부품 사양서(Spec. Sheet)` |
| 부품 설명 | 1 | 제목 칸이 `부품 설명`, 항목 이름이 구형 |
| **구매 목록** | 1 | 제목이 `구매 목록`. **사양서가 아니다.** 품번 칸에 `신규` 가 적혀 있다 |

## 4. 기획서의 물음표에 답한다 — "특정 폴더(D/B?)"

**폴더를 그대로 둔다. DB 로 옮기지 않는다.**

- 사양서를 만드는 사람은 앞으로도 엑셀로 만든다. DB 입력 화면을 만들면
  **일이 하나 늘 뿐** 아무도 쓰지 않는다.
- 대신 `scripts/build_index.py` 가 폴더를 훑어 **색인(JSON)** 을 만든다.
  폴더가 원본이고 색인은 언제든 다시 만들 수 있는 파생물이다.
  둘이 어긋나면 폴더가 옳다.
- 사이트는 색인만 읽는다. **엑셀 원본도 사진도 저장소에 넣지 않는다.**
  10건이 이미 4MB 다. 수천 건이면 GB 가 된다.
  표에는 어느 파일의 몇 번째 시트인지(`file`, `sheet`)만 남기고,
  실물은 지금 있는 자리에 둔다.
- 사내 공유 폴더 접근 권한이 없어도 **오늘 데모로 쓸 수 있어야 한다.**
  그래서 데모 모드(localStorage + 샘플 색인)를 함께 둔다.

## 5. 요구 기능 (기획서 원문 기준)

### 5.1 구글식 검색 한 칸
- 화면 가운데 검색창 하나. 그 외의 것은 넣지 않는다.

### 5.2 품번을 알 때
- `420108-02540` 입력 → 그 사양서 화면 출력

### 5.3 품번을 모를 때 — 되묻는다
- `호스 검색해줘` → 후보가 여럿이면
  *"산소호스와 실리콘 고열호스가 조회되었습니다. 어떤 걸 찾으시나요?"*
- `산소호스 알려줘` → 후보가 하나로 좁혀지면 사양서 출력
- **후보가 둘 이상일 때 하나를 골라 주지 않는다.** 고르는 것은 사람이다.

### 5.4 없을 때
- 기획자가 준 문구를 **그대로** 쓴다:
  *"저장 되어있는 파일이 없습니다. 내용을 재확인 하거나 생산기술팀에 사양서 등록을 요청 하세요"*

### 5.5 오늘 검색 기록 창
- 오늘 어떤 말로 검색했고 찾았는지/못 찾았는지 보는 작은 창.
- **못 찾은 검색이 이 시스템에서 가장 값진 자료다** — 등록해야 할 사양서 목록이다.

## 6. 반드시 규칙으로 막을 것 (테스트로 고정)

| # | 규칙 | 어디서 막는가 |
|---|---|---|
| 1 | 칸을 위치가 아니라 **라벨로** 찾는다 (품번이 B3 인 파일과 B4 인 파일이 있다) | `build_index.find_field` |
| 2 | 라벨 동의어를 한 항목으로 본다 (`메이커`=`제조 Maker`, `규격 및 모델`=`모델 및 규격`) | 같음 |
| 3 | 파일명과 내용의 품번이 다르면 **조용히 고르지 않고** 표시한다 | `build_index.resolve_part_no` |
| 4 | 사양서가 아닌 문서(구매 목록)를 사양서로 내주지 않는다 | `build_index.classify` |
| 5 | 못 읽은 칸을 지어내지 않는다. **'빈칸'과 '항목 자체가 없음'을 구분**한다 | `Logic.fieldState` |
| 6 | 후보가 둘 이상이면 하나를 고르지 않고 되묻는다 | `Logic.respond` |
| 7 | 없을 때 문구는 기획자가 준 그대로 | `Logic.NOT_FOUND` |
| 8 | 품번은 **6자리-5자리** | `Logic.isPartNo` + `check` 제약 |
| 9 | 검색 기록에 **개인정보를 남기지 않는다** | `Logic.maskContact` |
| 10 | 오늘 기록은 오늘 것만 | `Logic.todayLog` |

## 7. 산출물

- 빌드 없이 도는 순수 HTML/CSS/JS (GitHub Pages 배포)
- 데모 모드(localStorage) / Supabase 모드 — 화면과 규칙은 같고 저장 위치만 다르다
- `scripts/build_index.py` — 사내에서 폴더를 훑어 색인을 만드는 도구
- `test/*.test.js` (Node), `test/test_build_index.py` (Python),
  `scripts/sqltest/run.sh` (임시 PostgreSQL 에 스키마를 실제 적용)
- `README.md`, `docs/개발일지.md`

---

## 부록 A. 기획서 원문

> 안녕하세요 이애본 교수님
> 이번 AI역량강화 교육을 받고 있는 HD건설기계 류제찬 입니다
>
> 프로제트 관련하여
> 사양서 파일을 검색 및 내용 출력 해주는 사이트를 제작 하고 싶습니다
>
> 메인 서버 특정 폴더(D/B?)에 사양서를 엑셀 파일로 저장 해두고
> 사용자가 사이트에서 특정 키워드(품번 or 품명, 용도 등)으로 검색시 저장된 사양서
> 내용을 알려주거나 없을 경우 사양서 추가를 해야 한다는 안내 맨트를 출력 되었으면 합니다.
>
> EX)
> 구글 메인화면 처럼 검색 창이 있는 화면
>
>  1) 이용자가 품번 알고있다면 품번만 임력해서 조회하면  D/B에 저장된 사양서 화면 출력
>
>  2) 이용자가 필요한 제품을 검색 하고자 호스 검색해줘, 라고 입력하면 내부에 저장되어
> 있는 산소호스와 실리콘 고열 호스가 조회 되었습니다 어떤걸 찾으시나요? 질문,
> 산소호스 알려줘 라고하면 산소호스 사양서 화면 출력
>
>  3)  D/B에 저장되있지 않은 정보를 검색 하면 '저장 되어있는 파일이 없습니다.
> 내용을 재확인 하거나 생산기술팀에 사양서 등록을 요청 하세요' 맨트 출력 하기.
>
> 압축파일을 사양서 파일이고, 추가로 오늘 검색 요청 했던 리스트를 추가적으로
> 확인 할수있는 작은 창이 있으면 합니다

## 부록 B. 기획자가 보낸 첨부

`사양서 모음.zip` — 실제 사양서 10건 (.xls 3건 / .xlsx 7건).
**이 파일들은 저장소에 들어 있지 않다.** 개인정보가 들어 있고, 사내 자료다.
구조만 §3 으로 옮겨 적었고, 값은 `scripts/gen-seed.py` 가 만든 샘플을 쓴다.
